# TUI app definitions for omunchy
# Format: Name|Command|Icon
# Command is what gets wrapped as: $TERMINAL -e Command
# Icon is optional — falls back to a generic terminal icon if omitted.

htop|htop|utilities-system-monitor
lazygit|lazygit|git
nvim|nvim|nvim
