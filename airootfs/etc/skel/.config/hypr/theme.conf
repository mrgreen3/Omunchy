# Omunchy dark theme — Hyprland colours (default; swapped by `omunchy theme-set`)
# Palette: warm dark from Backgrounds/aesthetic.jpg (matches waybar/rofi/mako/foot)

general {
    col.active_border = rgba(adc6ffff)
    col.inactive_border = rgba(44474faa)
}

group {
    col.border_active = rgba(adc6ffff)
    col.border_inactive = rgba(44474faa)
}