# Makes hyprsunset do nothing to the screen by default.
# Without this, a fresh hyprsunset applies some tint to the monitor,
# which would make the bar's night-light indicator read as always-on.
# Started on demand by ~/.config/omunchy/bar/indicators.sh.
profile {
    time = 07:00
    identity = true
}