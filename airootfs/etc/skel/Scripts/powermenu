#!/bin/bash
choice=$(printf "󰍃  Logout\n󰑓  Reboot\n󰐥  Poweroff" | rofi -dmenu -p "Power" \
    -theme ~/.config/rofi/theme.rasi \
    -lines 3 \
    -theme-str 'inputbar { enabled: false; } listview { lines: 3; } window { width: 250px; }')

case "$choice" in
    *Logout)   hyprctl dispatch "hl.dsp.exit()" ;; # lua config: legacy 'dispatch exit' is a nil global to hl.dispatch
    *Reboot)   systemctl reboot ;;
    *Poweroff) systemctl poweroff ;;
esac
